import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marhaba.properties',
  appName: 'marhaba-properties',
  webDir: 'out'
};

export default config;
